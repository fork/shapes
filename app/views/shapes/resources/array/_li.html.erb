<% id = "#{path_to_css_selector(resource.path)}Ul" %>
<%= link_to 'New resource', select_resource_path(:id => resource.base.shape_id, :parent_path => resource.path) %>
<%= link_to 'Fold/Unfold', '#', { :onclick => "toggleArrayContent('#{ id }', '#{ resource.base.shape_id }', '#{ resource.path }')" } %>
<ul class="shapes<%= resource.class.name.demodulize %> shapesSortableList" id="<%= id %>" path="<%= resource.path %>" ></ul>
